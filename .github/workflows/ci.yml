name: CI

on:
  push:
    branches: [ main, mvp-scaffold ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"
      - name: Install & Test frontend
        run: |
          cd frontend
          npm ci
          npm run build --if-present
      - name: Install & Test backend
        run: |
          cd backend
          npm ci
          npm run build --if-present
